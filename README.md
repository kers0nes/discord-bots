# Roblox Scripts Discord Bot

A Discord bot that shares Roblox scripts.

## Commands
- `!scripts` - List all scripts
- `!get <number>` - Get a specific script

## Setup
1. Add `DISCORD_TOKEN` as environment variable
2. Run `npm install`
3. Run `npm start`
